STEPS TO RUN SCRIPT:
1)download files. 
2)goto file location.   eg: cd <file location>
3)chmod +x <filename>     eg: chmod +x script_nginx.sh [makes script executable]
4)./filename              eg: ./script_nginx.sh        [runs the script]

WHERE TO RUN WHICH SCRIPT FILES:
1) script_nginx.sh: at two webserver centos
2) script_haproxy.sh, script_testinghaproxy.sh & script_keepalived.sh : at two loadbalancer centos
